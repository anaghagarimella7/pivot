import { FilterColumnPipe } from './filter-column.pipe';

describe('FilterColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
